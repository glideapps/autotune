query($key: String!) {
    getApplication(key: $key) {
        name
        key
        experiments {
            name
            started
            epsilon
            options {
                name
                completed
                payoff
            }
        }
    }
}
